import React from 'react';
import { FormRequest } from '../features/form-requests/components/form-request/FormRequest';

export const FormPage: React.FC = () => {
  return (
    <FormRequest />
  );
};
